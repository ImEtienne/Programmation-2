int compteVote(int[][] vote, int candidat) {
    int nbrVote = 0;

    for (int i = 0; i < vote.length; i++)
        if (vote[i][0] == candidat)
            nbrVote += 1;
    return nbrVote;
}

void main() {}
