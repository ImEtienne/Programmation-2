void main() {}

int[] tabHasard(int n, int h) {
    int[] tab = new int[n];
    int rand;
    for (int i = 0; i < n; i++) {
        rand = random(0, h);
        tab[i] = rand;
    }
    return tab;
}
