void main() {}

void rotation(int[] tab) {
    if (tab.length > 0) {
        int j = tab[tab.length - 1];
        for (int i = tab.length - 1; i > 0; i--) {
            tab[i] = tab[i - 1];
        }
        tab[0] = j;
    }
}
