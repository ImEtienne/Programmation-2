void main() {
    int[][] tab = new int[5][5];
    for (int i = 0; i < tab.length; i++) {
        for (int j = 0; j < tab.length; j++) {
            // tab[i][j] = 0;
            print(tab[i][j]);
        }
        println();
    }
}
