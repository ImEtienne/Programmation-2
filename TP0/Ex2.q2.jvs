void main() {
    clear();
    // int n = readInt();
    int[] tab = new int[] {8, 2, 2, 0, 3, 6};
    int s = 0;
    int min = tab[0];
    for (int i = 0; i < tab.length; i++) {
        if (tab[i] < min) {
            min = tab[i];
        }
        s = s + tab[i];
    }
    println("somme :" + s);
    println("mminimum:" + min);
}
